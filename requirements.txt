# AgentComet SDK — Python Dependencies
# ======================================

# Core (required)
requests>=2.28.0
pyyaml>=6.0
uaf-cli>=0.8.0

# LLM Providers (optional — install only what you need)
# openai>=1.0.0              # For OpenAIChat provider
# google-genai>=1.0.0        # For Gemini provider (recommended)
# google-generativeai>=0.3   # For Gemini provider (legacy)
# anthropic>=0.20.0          # For Anthropic provider
# langchain-ollama>=0.1.0    # For Ollama.as_langchain() interop only